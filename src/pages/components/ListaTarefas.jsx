import React, { useEffect, useState, useRef } from 'react';
import { supabase } from '../../supabaseClient';
import '../static/HomePageUser.css';

export default function ListaTarefas({ tarefaId, userId }) {
 return (
    <></>
 )
}